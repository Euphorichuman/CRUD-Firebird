<% include partials/_header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <td>N°</td>
            <td>Identificación</td>
            <td>Nombre</td>
            <td>Opciones</td>
          </tr>
        </thead>
        <tbody>
          <% if (data) { %>
            <% for(var i = 0; i < data.length; i++) { %>
              <tr>
                <td><%= (i + 1) %></td>
                <td><%= data[i].ID %></td>
                <td><%= data[i].NOM %></td>
                <td>
                  <a href="/updatepadre/<%= data[i].ID %>" class="btn btn-primary">Editar</a>
                  <a href="/deletepadre/<%= data[i].ID %>" class="btn btn-danger">Borrar</a>
                </td>
              </tr>
            <% } %>
          <% } %>
        </tbody>
      </table>
    </div>
    <div class="col-md-5">
      <div class="card-body">

        <form action="/addpadre" method="POST">
          <div class="form-group">
            <input type="number" name="id" placeholder="Identificación" class="form-control" autofocus>
          </div>
          <br>
          <div class="form-group">
            <input type="text" name="nom" placeholder="Nombre" class="form-control" autofocus>
          </div>
          <br>
          <button type="submit" class="btn btn-primary">
            Agregar padre
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<% include partials/_footer %>
